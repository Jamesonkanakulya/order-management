# Database - uses same DB as appointment_agent
DB_PASSWORD=changeme

# GitHub Token for AI
GITHUB_TOKEN=

# Database URL (uses same database as appointment_agent)
DATABASE_URL=postgresql+asyncpg://appuser:changeme@db:5432/appointment_agent
